# ملفات الأدوات المساعدة

هذا المجلد يحتوي على الأدوات والمساعدات المختلفة للتطبيق.

## الملفات

### 🎵 `audioGenerator.ts`
**مولد الأصوات الرئيسي**
- يولد الأصوات باستخدام Web Audio API
- يدير التأثيرات الصوتية المتقدمة
- يربط مع audioManager للتحكم في الصوت

**المميزات:**
- أصوات مولدة برمجياً (لا تحتاج ملفات خارجية)
- تأثيرات reverb و tremolo
- مرشحات صوتية متقدمة

### 🔊 `audioManager.ts`
**مدير الصوت المركزي**
- يدير مستوى الصوت العام
- يحفظ ويستعيد إعدادات الصوت
- يتحكم في جميع العناصر الصوتية

**المميزات:**
- تحكم مركزي في جميع الأصوات
- حفظ تلقائي في localStorage
- دعم HTML Audio و Web Audio API

### 💾 `gameStorage.ts`
**نظام حفظ بيانات اللعبة**
- يحفظ النتائج والإحصائيات
- يحسب المتوسطات والأرقام القياسية
- يدير بيانات اللاعب

**البيانات المحفوظة:**
- أفضل نتيجة
- إجمالي عدد الألعاب
- إجمالي النقاط المجموعة
- متوسط النقاط لكل لعبة

## استخدام الملفات

### في مكونات React:

```typescript
import { audioGenerator } from '../utils/audioGenerator';
import { audioManager } from '../utils/audioManager';
import { GameStorage } from '../utils/gameStorage';

// تشغيل صوت
audioGenerator.playClickSound();

// التحكم في الصوت
audioManager.setMasterVolume(0.5);

// حفظ نتيجة
GameStorage.saveBestScore(500);
```

### في hooks:

```typescript
import { useSoundEffect } from '../hooks/useAudio';

// استخدام التأثيرات الصوتية
const { playClick, playVictory } = useSoundEffect();
```

## ملاحظات مهمة

- جميع الأصوات تتأثر بإعدادات audioManager
- البيانات تُحفظ تلقائياً في localStorage
- الأصوات المولدة لا تحتاج ملفات خارجية
- النظام متوافق مع جميع المتصفحات الحديثة
